<!-- Copyright (c) Aghyad khlefawi. All rights reserved.  
     Licensed under the MIT License. See LICENSE file in the project root for full license information.-->
<UserControl x:Class="HR.Clients.WPF.Companies.Editors.EmployeeEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HR.Clients.WPF.Companies.Editors"
             xmlns:controls="http://schemas.Coddee.com/2017/WPF"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <local:EmployeeEditorViewModelBase x:Key="designData" />
    </UserControl.Resources>
    <Grid d:DataContext="{StaticResource designData}">
        <controls:Form>
            <controls:Form.TitleStyle>
                <Style TargetType="Label">
                    <Setter Property="FontWeight" Value="Bold"/>
                </Style>
            </controls:Form.TitleStyle>
            <controls:FormField Title="First name">
                <TextBox Text="{Binding FirstName,UpdateSourceTrigger=PropertyChanged}" />
            </controls:FormField>
            <controls:FormField Title="Last name">
                <TextBox Text="{Binding LastName,UpdateSourceTrigger=PropertyChanged}" />
            </controls:FormField>
        </controls:Form>
    </Grid>
</UserControl>
