using System;
using System.Collections.Generic;
using System.Text;

namespace Coddee.Core
{
    public class SyncActions
    {
        public const string SyncReceived = nameof(SyncReceived);
        public const string SyncItem = nameof(SyncItem);
    }
}
